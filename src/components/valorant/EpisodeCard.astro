---
interface Props {
  uuid: string;
  assetObjectName: string;
}

const { uuid, assetObjectName } = Astro.props;
const shortAssetObjectName = assetObjectName.split('_')[0]; // Esto te da "Episode1"

---

<a href={`/valorant/competitive/${uuid}`} class="bg-valorant-dark p-4 rounded-lg shadow-lg block hover:shadow-2xl transition-shadow duration-300">
  <h2 class="text-2xl text-valorant hover:underline text-center">{shortAssetObjectName}</h2>
</a>
